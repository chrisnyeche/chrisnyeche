import { Box } from "@chakra-ui/react";
import Carousel from "../plugins/Carousel";

const Projects = () => {
  return (
    <Box as={"section"}>
      <Carousel />
    </Box>
  );
};

export default Projects;
